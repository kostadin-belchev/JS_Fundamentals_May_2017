function lastKNumsSequence (params) {
    let [n, k] = params;
    let result = [];
    for (let i = 1; i <= n; i++) {
        const element = array[i];
        for (let j = 0; j < k; j++) {
            const element = array[j];
            
        }
        result += element;
    }
}