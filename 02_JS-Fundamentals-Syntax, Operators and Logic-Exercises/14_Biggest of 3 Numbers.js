function biggestOfThreeNums ([num1, num2, num3]) {
    console.log(Math.max(num1, num2, num3));
}
// (nums) => Math.max(nums[0], nums[1], nums[2]); also correct solution in Judge

biggestOfThreeNums([5,-2, 7]);
biggestOfThreeNums([-10,- 20, -30]);

